@using TatilSeyahatMVC.Models.Siniflar
@model List<Blog>

     
    @*sol partial*@
<div class="padding1">
    <h4>En Beğendiğim Yerler</h4>
    <div class="box-left">
        @foreach (var x in Model)
        {
            <div class="bot-img">
                <img src="@x.BlogImage" alt="" />
            </div>
            <div class="bot-desc">
                <h3>@x.Header</h3>
            </div>
            <div class="clear"></div>
        }
    </div>

    @*sağ partial*@
  @Html.Action("OurbestTwo","Default")
</div>